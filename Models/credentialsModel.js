export default class Credentials{
    constructor(
        userId,
        token,
        password
    ){
        this.userId = userId;
        this.token = token;
        this.password = password;
    }
}